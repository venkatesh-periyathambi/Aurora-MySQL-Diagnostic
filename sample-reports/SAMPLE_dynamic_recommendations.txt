=== DYNAMIC RECOMMENDATIONS (based on detected conditions) ===

--- DEADLOCK ANALYSIS ---
deadlock_status:
  [CRITICAL] 81 deadlocks detected. Immediate action required.

deadlock_remediation:
  (1) Run SHOW ENGINE INNODB STATUS and check LATEST DETECTED DEADLOCK section for the exact cycle.
  (2) Ensure all transactions access tables/rows in the SAME ORDER.
  (3) Keep transactions short - commit frequently.
  (4) Add indexes to reduce lock footprint.
  (5) Consider READ-COMMITTED isolation to eliminate gap locks.

--- ROW LOCK CONTENTION ANALYSIS ---
lock_wait_status:
  [CRITICAL] 324 row lock waits. Heavy contention.

wait_times:
  Average wait: 4552ms | Max wait: 30368ms

lock_remediation:
  (1) Check data_lock_waits output above for blocking transaction pairs.
  (2) Identify hot rows/indexes being contended.
  (3) Reduce transaction duration.
  (4) Add covering indexes.
  (5) Consider optimistic locking (retry on deadlock).

--- GAP LOCK ANALYSIS ---
gap_lock_status:
  [WARNING] 29 gap locks detected. May cause parallel INSERT/DELETE contention.

gap_lock_remediation:
  (1) Switch to READ-COMMITTED isolation: SET GLOBAL transaction_isolation = "READ-COMMITTED"
      (eliminates gap locks for non-locking reads).
  (2) Use unique/exact-match WHERE clauses instead of ranges.
  (3) For INSERT-heavy workloads, ensure innodb_autoinc_lock_mode=2.

current_isolation_level:
  REPEATABLE-READ

--- LONG TRANSACTION ANALYSIS ---
long_trx_status:
  [CRITICAL] Longest active transaction: 45s. Long transactions hold locks and block others.

long_trx_remediation:
  (1) Identify the long transaction from INNODB_TRX output above.
  (2) Break large transactions into smaller batches.
  (3) Avoid SELECT ... FOR UPDATE on large result sets.
  (4) Set innodb_lock_wait_timeout lower (e.g., 10s) to fail fast.
  (5) Implement application-level retry on ER_LOCK_WAIT_TIMEOUT.

active_transactions: 74
total_rows_locked_across_all_trx: 15832

--- LOCK WAIT TIMEOUT ANALYSIS ---
timeout_status:
  [WARNING] 3 lock wait timeouts detected.

current_setting:
  Current innodb_lock_wait_timeout = 50s

timeout_remediation:
  (1) Identify blocking transactions (see data_lock_waits above).
  (2) Kill long-blocking transactions if acceptable.
  (3) For parallel batch jobs, lower timeout to 5-10s and implement retry logic.
  (4) Fix root cause: missing indexes, wide range locks, or long transactions.

--- METADATA LOCK ANALYSIS ---
mdl_status:
  [OK] No pending metadata locks.
mdl_remediation:
  No action needed.

--- CONNECTION SATURATION ANALYSIS ---
connection_status:
  [WARNING] 85/150 connections used (57%).

connection_remediation:
  (1) Increase max_connections in parameter group.
  (2) Use connection pooling (RDS Proxy, ProxySQL, application-side pool).
  (3) Close idle connections.
  (4) Check for connection leaks in application code.

--- PURGE LAG ANALYSIS ---
purge_status:
  [OK] History list length: 8201.
purge_remediation:
  No action needed.

--- AUTO-INCREMENT LOCK MODE ---
autoinc_status:
  [OK] innodb_autoinc_lock_mode=2 (interleaved). Best for parallel insert performance.
autoinc_remediation:
  No action needed.
current_value: 2

--- OVERALL ASSESSMENT ---
instance_health_summary:
  Deadlocks: 81 | Row lock waits: 324 | Lock timeouts: 3 | Active trx: 74 | Max trx age: 45s | History list: 8201 | Threads connected: 85/150

==============================================================================
SEVERITY LEGEND:
  [CRITICAL] - Immediate action required. Impacting availability/performance.
  [WARNING]  - Investigate soon. May escalate under load.
  [INFO]     - Informational. Monitor for changes.
  [OK]       - Healthy. No action needed.
==============================================================================
